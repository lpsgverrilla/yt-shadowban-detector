name: Build and Release

on:
  push:
    tags:
      - 'v*.*.*'  # Triggers on version tags like v1.0.0, v2.1.3, etc.

permissions:
  contents: write  # Required to create releases

jobs:
  build-and-release:
    runs-on: windows-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pytest pytest-cov
          pip install pyinstaller

      - name: Run tests
        run: python -m pytest tests/ -v

      - name: Build executable with PyInstaller
        run: python -m PyInstaller build.spec

      - name: Get release version
        id: get_version
        run: echo "VERSION=${GITHUB_REF#refs/tags/}" >> $GITHUB_OUTPUT
        shell: bash

      - name: Create release package
        run: |
          mkdir yt-echo-test
          copy README.md yt-echo-test\
          copy LICENSE yt-echo-test\
          copy dist\yt-echo-test.exe yt-echo-test\
          tar -a -c -f yt-echo-test-${{ steps.get_version.outputs.VERSION }}.zip yt-echo-test
        shell: cmd

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: yt-echo-test-${{ steps.get_version.outputs.VERSION }}.zip
          body: |
            ## yt-echo-test ${{ steps.get_version.outputs.VERSION }}

            ### Installation
            1. Download `yt-echo-test-${{ steps.get_version.outputs.VERSION }}.zip`
            2. Extract the zip file
            3. Run `yt-echo-test.exe`

            ### What's Included
            - `yt-echo-test.exe` - Windows executable
            - `README.md` - Documentation
            - `LICENSE` - License information
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
